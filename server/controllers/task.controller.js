const Project = require('../models/project.model');

module.exports = function createTask(req, res) {
    
};

module.exports = function getTaskInfo(req, res) {
    
};

module.exports = function updateTaskInfo(req, res) {
    
};